import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    paddingLeft: 12,
    fontSize: 24,
    fontWeight: "700",
  },
  container: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: "100%",
    paddingBottom: "28%",
  },
});
